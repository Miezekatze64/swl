include "../../std.swl";
char NULL = as[char, 0];
    
func main() -> int {
    string buf = arr[char, 100];
    while (true) {
        int bytes = get_line(buf);
        buf[bytes] = NULL;
        system(buf);
    }
    <- 0;
}
