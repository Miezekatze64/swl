// include "std.swl";

// #link "test.o"
#link_lib "libc"
extern func malloc(int size) -> long;
// extern func f(char *a, int b, int c, int d, int e, int f) -> int;

struct Test {
    int a;
    int b;
}

func main() -> int {
    if (1 == 4) {
        <- 1;
    } else if (2 == 2) {
        <- 2;
    } else {
        <- 3;
    }

    <- 0;   //f(as[char*, 0], 2, 3, 4, 5, as[int, l]);
}
